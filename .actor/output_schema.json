{
  "actorOutputSchemaVersion": 1,
  "title": "Job openings across every supported ATS",
  "description": "One row per job opening. If outputMode is companies or both, the rows with recordType=company are one-per-company hiring summaries. Rows with recordType=notFound name a company that could not be read and why.",
  "properties": {
    "results": {
      "type": "string",
      "title": "Job openings",
      "description": "Every row the run produced, across all the companies asked for.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    }
  }
}
